{
    "name": "company-manager",
    "version": "1.0.0",
    "description": "A simple Express app to manage companies",
    "main": "server.js",
    "scripts": {
      "start": "npm run reset && nodemon --require dotenv/config server.js",
      "reset": "cd config && node --require dotenv/config reset.js"
    },
    "dependencies": {
      "express": "^4.17.1",
      "pg": "^8.6.0",
      "dotenv": "^10.0.0"
    },
    "devDependencies": {
      "nodemon": "^2.0.7"
    }
  }
  